public class InstanceVariable {
    
    int b = 4;

    public static void main(String[] args) {
        
        System.out.println(b);

        /* Gives error because instance Variable can't be accessed with in methods directly. There is a way to call them
           They can only be accessed by making objects
        */
    }
}
